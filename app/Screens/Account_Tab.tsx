import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Account_Tab = () => {
  return (
    <View style={{flex:1, backgroundColor:'blue'}}>
      <Text>Account_Tab</Text>
    </View>
  )
}

export default Account_Tab

const styles = StyleSheet.create({})